<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nuevaapp12"
        tools:targetApi="31">
        <activity
            android:name=".AuthSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <!-- Intent filter and other activity configuration here -->
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <!-- Intent filter and other activity configuration here -->
        </activity>
        <activity
            android:name=".FiveLatestDataActivity"
            android:exported="true">
            <!-- Intent filter and other activity configuration here -->
        </activity>
        <activity
            android:name=".LuminosityActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".PostLoginActivity"
            android:label="PostLoginActivity">
        </activity>
        <activity
            android:name=".WebViewActivity"
            android:label="WebViewActivity">
        </activity>


    </application>

</manifest>